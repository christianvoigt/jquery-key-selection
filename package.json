{
	"name": "jquery-key-selection",
	"title": "jQuery Key Selection",
	"description": "jQuery keySelection is a lightweight jQuery plugin (3kb) for cycling through arbitrary element collections with arrow keys. Collection members are selectable by keyboard or mouse.",
	"author": "Christian Voigt",
	"homepage": "https://github.com/christianvoigt/jquery-key-selection/",
	"version": "1.0",
	"devDependencies": {
		"grunt": "~0.4.1",
		"grunt-cli": "~0.1.7",
		"grunt-contrib-jshint": "~0.4.3",
		"grunt-contrib-concat": "~0.3.0",
		"grunt-contrib-uglify": "~0.2.0",
		"grunt-contrib-coffee": "~0.7.0"
	},
	"scripts": {
		"test": "grunt travis --verbose"
	}
}
